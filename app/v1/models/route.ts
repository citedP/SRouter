import{NextRequest,NextResponse}from"next/server";import{secretFrom}from"@/lib/auth";import{loadVault}from"@/lib/vault";import{enforceRateLimit}from"@/lib/request";export async function GET(r:NextRequest){try{await enforceRateLimit(r,"models",120,60);const v=await loadVault(secretFrom(r)),ids=new Set([...Object.keys(v.routes),...v.providers.filter(p=>p.enabled).flatMap(p=>p.models)]);return NextResponse.json({object:"list",data:[...ids].map(id=>({id,object:"model",owned_by:"srouter"}))},{headers:{"cache-control":"no-store"}})}catch(e){const m=e instanceof Error?e.message:"error";return NextResponse.json({error:{message:m}},{status:m==="RATE_LIMITED"?429:401})}}
